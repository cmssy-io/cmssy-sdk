# Settings → Headless in the cmssy dashboard
CMSSY_ORG_SLUG=
CMSSY_WORKSPACE_SLUG=

# Also Settings → Headless. The editor proves itself with this secret, so an
# unverified ?cmssyEdit=1 cannot open the door.
CMSSY_DRAFT_SECRET=
