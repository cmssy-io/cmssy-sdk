---
import { loadCmssyPage } from "@cmssy/astro";
import { resolveEditorBlockData } from "@cmssy/react";
import { cmssy } from "../cmssy.config";
import { blocks } from "../cmssy/blocks";
import { Blocks } from "../components/Blocks";
import LayoutSlot from "../cmssy/layout-slot";

export const prerender = false;

const { page, layouts, locale, defaultLocale, enabledLocales } = await loadCmssyPage(
  cmssy,
  Astro.request,
  Astro.url,
  { blocks },
);

if (!page) {
  Astro.response.status = 404;
}

const { data: blockData, content: blockContent } = await resolveEditorBlockData({
  page,
  blocks,
  locale,
  defaultLocale,
  enabledLocales,
  config: cmssy,
});
---

<html lang={locale}>
  <head>
    <meta charset="utf-8" />
    <title>{page?.seoTitle?.[locale] ?? page?.displayName?.[locale] ?? "cmssy on Astro"}</title>
  </head>
  <body>
    <LayoutSlot
      groups={layouts}
      position="header"
      locale={locale}
      defaultLocale={defaultLocale}
      enabledLocales={enabledLocales}
    />
    {
      page ? (
        <Blocks
          page={page}
          locale={locale}
          defaultLocale={defaultLocale}
          enabledLocales={enabledLocales}
          blockData={blockData}
          blockContent={blockContent}
        />
      ) : (
        <main><h1>Not found</h1></main>
      )
    }
    <LayoutSlot
      groups={layouts}
      position="footer"
      locale={locale}
      defaultLocale={defaultLocale}
      enabledLocales={enabledLocales}
    />
  </body>
</html>
